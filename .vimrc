""""""""""""""""""""
" .vimrc
" Taylor J. Smith
""""""""""""""""""""

set nocompatible            "no vi compatibility mode
syntax enable               "enable syntax highlighting

"set background=dark        "set background
"colorscheme solarized      "set colour scheme

filetype plugin indent on   "enable filetype indenting

set autoindent              "enable autoindenting
set expandtab               "quietly convert tabs to spaces
set hlsearch                "enable highlighting when searching
set ignorecase              "disable case-sensitivity when searching
set laststatus=2            "enable status line
set noerrorbells            "disable error bells
set novisualbell            "disable visual bells
set nowrap                  "disable text wrapping
set number                  "enable line numbering
set ruler                   "enable info at bottom
set shiftwidth=4            "set indent width
set shiftround              "enable indenting to nearest tab stop
set softtabstop=4           "set tab stop width
set smarttab                "use tabs at beginning of line, spaces elsewhere
set tabstop=4               "set tab stop width
set t_vb=                   "disable screen flashing
